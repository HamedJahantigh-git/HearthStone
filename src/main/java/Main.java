import CLI.CLI;
import logs.PlayerLogs;


public class Main {
    public static void main(String[] args) {
        String[] s = new String[0];

        CLI game = new CLI();
        game.startGame();
    }
}
